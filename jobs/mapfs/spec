---
name: mapfs

templates:
  install.erb: bin/pre-start
  bpm.yml.erb: config/bpm.yml

packages:
- mapfs
- mapfs-fuse

provides:
- name: mapfs
  type: mapfs
  properties:
  - path

properties:
  disable:
    description: "Disable mapfs"
    default: false
  path:
    description: "Path to mapfs binary"
    default: /var/vcap/jobs/mapfs/bin/mapfs
