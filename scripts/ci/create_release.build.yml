---
platform: linux

rootfs_uri: docker:///cloudfoundry/diego-pipeline

inputs:
  - name: mapfs-release
  - name: mapfs-version

outputs:
  - name: created-plugins-release

params: []

run:
  path: mapfs-release/scripts/ci/create_release
